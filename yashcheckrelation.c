#include<stdio.h>
int main()
{  
  int a=20;
  int b=10;
  printf("are both value equal ? %d", a==b);
  printf("\n is a  greater than b ? %d  ", a>b);
  printf("\n is a less than b ? %d  ", a<b);
  printf("\n is a  not equal to b ? %d  ", a!=b);
    printf("\n is a  greater than or equal to  b ? %d  ", a>=b);
      printf("\n is a  lesser than or equal to  b ? %d  ", a<=b);
  return 0;
}	
